[package]
name = "abcd-file-sys"
description = "C FFI bindings for ArkCompiler .abc file format (libpandafile)"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]

[build-dependencies]
cc = "1"
bindgen = "0.71"
