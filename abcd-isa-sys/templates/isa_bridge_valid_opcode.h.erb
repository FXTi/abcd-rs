// Autogenerated file -- DO NOT EDIT!
// Opcode validity check, generated from isa.yaml (audited fix: decode must
// return an error on unassigned opcodes instead of aborting in GetFormat).

/* Returns 1 if `opcode` is assigned in the vendored ISA table, else 0. */
static int isa_opcode_is_valid(uint16_t opcode) {
    switch (opcode) {
% Panda::instructions.each do |insn|
    case <%= insn.opcode_idx %>:
% end
        return 1;
    default:
        return 0;
    }
}
