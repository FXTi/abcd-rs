# Architecture Overview

## Positioning

abcd-rs is a Rust toolkit for ArkCompiler ABC bytecode (`.abc`) files: **read, write, inspect, and transform through an SSA IR**. It is not a decompiler (the first-generation decompile direction has been removed). The goal is to make the foundation up to the IR layer solid: anyone building an optimizer, an analyzer, a patching tool, or a future decompiler on top should be able to stand directly on the IR.

## Repository layout

```
abcd-rs/
├── design/                  # This directory: design documents
├── abcd-isa-sys/            # ISA FFI layer (Ruby codegen + C++ bridge + bindgen)
├── abcd-isa/                # ISA safe Rust API (decode/encode/version/classification)
├── abcd-file-sys/           # Container-format FFI layer (libpandafile + C bridge + bindgen)
├── abcd-file/               # Container safe Rust API (decode/encode/Builder)
└── abcd-ir/                 # SSA intermediate representation (lift / opt / lower)
```

Five crates, all libraries. Strict layering:

```
abcd-isa-sys ──▶ abcd-isa ──┐
                             ├──▶ abcd-file ──▶ abcd-ir
abcd-file-sys ───────────────┘
```

That is: `abcd-ir` depends on `abcd-file` + `abcd-isa`; `abcd-file` depends on `abcd-file-sys` + `abcd-isa`; the two `-sys` crates are leaves.

## Data flow

```
.abc bytes
   │  abcd-file::decode          (bytecode decoded via abcd-isa)
   ▼
File (owned: classes / literal_arrays / entity_map / StringPool)
   │  abcd-ir::lift::lift_file
   ▼
Module (SSA IR: functions / insts / blocks / values)
   │  abcd-ir::opt::optimize_module
   ▼
Module (optimized)
   │  abcd-ir::lower::lower_function (per function: regalloc → isel → layout)
   ▼
Bytecode sequence + try blocks
   │  abcd-file::encode / Builder
   ▼
.abc bytes
```

Key invariant: **the IR preserves all metadata** (four annotation categories, debug info, try regions, function kind, access flags), serving semantic round-trip fidelity. Byte-level round-trip is not a goal (the builder decides its own layout).

## Version-aware layering

arkcompiler's ISA carries no per-opcode version annotations; es2panda hard-codes version gates at the compiler semantic level (e.g. `SENDABLE_CLASS_MIN_SUPPORTED_API_VERSION = 11`). We split that responsibility across three layers:

| Layer | Crate | Responsibility |
|-------|-------|----------------|
| Version queries | abcd-isa | `Version`: current/min/API mapping/compatibility/blocklist |
| File layout | abcd-file | Version-conditional section layout (e.g. before 12.0.6.0 the literal-array index lives in the header) |
| Opcode selection | abcd-ir | Lowering picks opcodes by target API level (see ir.md) |

Principle: `abcd-isa` only faithfully encodes whatever opcode the caller gives it; it never decides "which opcode to use".

## Current status

| Crate | Read | Write | Notes |
|-------|------|-------|-------|
| abcd-isa-sys | ✅ | ✅ (emitter FFI) | Generated by the Ruby pipeline |
| abcd-isa | ✅ | ✅ | Long jumps auto-widened by the C++ emitter |
| abcd-file-sys | ✅ | ✅ (builder FFI) | ~170 bridge functions |
| abcd-file | ✅ | ⚠️ | `encode()` semantic round-trip implemented but disabled (C++ dedup crash; see file-format.md known limitations) |
| abcd-ir | ✅ lift | ✅ lower | Optimization pipeline usable; several approximations (see ir.md known gaps) |

## Reference sample

The development baseline is `modules.abc` (21.6MB, version 12.0.6.0, 2035 classes / 29147 literal arrays / 15738 line-number programs / 2 index headers). It is not distributed with the repository (`.gitignore`'s `*.abc`).
