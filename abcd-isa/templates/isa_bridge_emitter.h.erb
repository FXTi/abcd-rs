/*
 * Auto-generated C bridge for BytecodeEmitter per-mnemonic emit functions.
 * Included by isa_bridge.cpp.
 */

// Autogenerated file -- DO NOT EDIT!

% Panda::instructions.group_by(&:mnemonic).each do |mnemonic, group|
%   emitter_name = group.first.emitter_name
%   is_jump = group.first.jump?
%   signature = emitter_signature(group, is_jump)
%   c_name = "isa_emit_" + mnemonic.tr('.', '_')
%
%   # Build C parameter list
%   c_params = ["IsaEmitter* e"]
%   call_args = []
%   signature.each do |o|
%     if o.type == 'const Label &'
%       c_params << "uint32_t label_id"
%       call_args << "e->labels[label_id]"
%     elsif o.type.start_with?('int')
%       c_params << "#{o.type} #{o.name}"
%       call_args << o.name
%     else
%       c_params << "#{o.type} #{o.name}"
%       call_args << o.name
%     end
%   end
%
void <%= c_name %>(<%= c_params.join(', ') %>) {
    e->emitter.<%= emitter_name %>(<%= call_args.join(', ') %>);
}

% end
